function OrderItem({ item }) {
    // console.log(item.name)
    return (
        <div>
            {/* <p className='text-slate-300 font-semibold text-sm'>{item.name}</p> */}
        </div>
    )
}

export default OrderItem
